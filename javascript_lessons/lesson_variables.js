let lesson_title = 'Lesson: variables';
console.log(lesson_title);
    //var or let: let has a smaller scope, 
    //so they are limited to 
    //the block, statement or expression 
    //on which it is used
        // variable declaration
        let age;
        // variable assignment
        age = 50;
        //show in console
        console.log(age)
        //variable declaration and assignment
        let name = 'Pilot'; //string
        console.log(name);
        //boolean variable (true or false)
        let hasExperience = true
        console.log(hasExperience);
        //number
        let examResult = 50.5;
        console.log(examResult);
        //undefined
        let something;
        console.log(something);